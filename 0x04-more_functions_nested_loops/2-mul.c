#include "main.h"

/**
* mul - Multiplies two digits
* @x: The first number to multiply
* @y: the second number to multiply with first number.
* Return: the results of multiplication between x and y param
*/

int mul(int x, int y)
{
return (x * y);
}

