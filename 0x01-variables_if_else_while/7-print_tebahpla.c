#include <stdio.h>
/**
* main - Prints alphabet in reverse
* Return: Always 0 (success)
*/
int main(void)
{
	char le;

	for (le = 'z'; le <= 'z'; le--)
		putchar(le);

	putchar('\n');

	return (0);
}
